---
const { href, variant = 'secondary', type = 'button' } = Astro.props;
const className = `btn btn-${variant}`;
---
{href ? <a class={className} href={href}><slot /></a> : <button class={className} type={type}><slot /></button>}
